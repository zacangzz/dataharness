# TUI screens module — kept for backward import compatibility.
# `ApprovalScreen` and `ClarificationScreen` were removed in favor of inline
# `ApprovalBanner` / `ClarificationBar` widgets (see app/tui/widgets.py).
